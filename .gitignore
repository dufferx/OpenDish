# Dependencies
node_modules/

# Build output
dist/
**/dist/

# Env files (see .env.example)
.env
.env.local
**/.env
**/.env.local
# Backups written by `pnpm setup:local -- --backup-managed-env` before it
# overwrites an existing managed-profile `.env.local`; these can contain a
# managed Supabase URL/publishable key and must never be committed.
**/.env.local.managed-backup-*
!**/.env.example

# Supabase local
supabase/.branches/
supabase/.temp/
supabase/functions/.env

# Test artifacts
playwright-report/
test-results/
coverage/

# Tooling caches
*.tsbuildinfo
.vite/

# Local specification and AI tooling
.kimi-code/
.specify/
.specs/
specs/