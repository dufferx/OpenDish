# OpenDish web app

React and Vite frontend for OpenDish. Run it from the repository root with:

```bash
pnpm dev
```

Environment variables are documented in the root [`.env.example`](../../.env.example).
